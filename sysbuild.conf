# Sysbuild top-level configuration for nRF5340 dual-core build.
# Net core runs the NCS `ipc_radio` sample configured as BLE HCI transport.

SB_CONFIG_NETCORE_IPC_RADIO=y
SB_CONFIG_NETCORE_IPC_RADIO_BT_HCI_IPC=y
