# Day 9 — Sim-path verification requirements + invitation gate

**日期**: 2026-04-20 (Linux Day 5)
**迭代日**: Day 9
**里程碑**: M4 收尾重整 —— 把"面试官邀请"这一步重新 gated 在 sim-path 可验证之上

---

## 晨会计划

### 昨日回顾（Day 8）
- ✅ CI 在 `f0d1a87` 和 `2c103bc` 连续两次 GREEN，workflow 稳定
- ✅ README 补齐 AC-R2 / bsim / Ubuntu 24.04 LD 警告
- ✅ 本地 `verify-acceptance.sh` 24/24 PASS
- ⏸️ 原计划的"面试官邀请"被用户主动推迟

### 今日目标（用户明示）
用户认为：当前的"交付物"对 **无硬件的面试官** 来说证据不够强 —— 纯 clean build 只证明配置正确，不证明 BLE HRS 运行期行为正确。因此发邀请前要把"sim 镜像可跑通 BLE"这条再闭合一次。明确要求：

1. **Hardware build image clean** —— 已经 24/24 守住，**regression-only**
2. **Sim image working with BLE simulation** —— 新 AC，需要 bsim↔bsim 跑通 HRS peripheral ↔ HRS central
3. **Sim image verified by Chrome BLE** —— 新 AC，需要 native_sim + HCI user-channel + Chrome Web Bluetooth 端到端看到心率
4. **把规则固化进 `.github/agents/dev-workflow.agent.md`**：邀请 gate
5. **把状态跟踪进 `REQUIREMENTS.md`**

### 今天只做结构性工作（实际验证推到 Day 10+）
实现 AC-V1（bsim run）和 AC-V2（native_sim+Chrome）都不是一天能 close 的任务 —— 需要 bsim central pair、native_sim+userchan 配置、USB BLE dongle 测试、Patchright 脚本打磨。今天的范围：

- ✅ 把两条新 AC 立起来 + 骨架脚本（skip-when-deps-missing）
- ✅ 规则固化
- ✅ 状态追踪表
- ⏸️ 实际跑通留给 Day 10（bsim run）和 Day 11+（Chrome BLE）

### 风险
- 用户选 "Both" —— 需要两次独立的 sim 路径（bsim 和 native_sim），两套配置，两个构建目录。不要混淆。
- Chrome Web Bluetooth 同机双 adapter 的 BlueZ 配置可能要 `rfkill` / `hciconfig hciN up` 手工挑，当天跑通前不能保证
- 不要因为加了新 AC 反而 break 现有 24/24

### 验收检查点
- [ ] `REQUIREMENTS.md` §6.3 加上 AC-V1 + AC-V2 + 状态表
- [ ] `.github/agents/dev-workflow.agent.md` 加邀请 gate + 更新 P2 列表
- [ ] `verify-acceptance.sh` 加 AC-V1 + AC-V2 skip-unless-ready 段
- [ ] 本地回归仍然 24/24 PASS（新两条 SKIP）
- [ ] Day 9 commit + push + CI GREEN

---

## 执行记录

（见 commit 信息 + §§ 以下小节）

### 1. `REQUIREMENTS.md` §6 扩充
- 在原有 §6.3 之后补 §6.3.2 "硬件缺席下的运行期 BLE 验证（Day 9+ 新增）"
- 两条新 AC：
  - **AC-V1** `bsim run: HRS peripheral ↔ HRS central, notify visible` — BabbleSim 两个 zephyr.exe 通过 `bs_2G4_phy_v1` 对接，central 侧日志看到 HR notification
  - **AC-V2** `native_sim + Chrome Web Bluetooth: end-to-end HR notify` — 用 `native_sim` + `CONFIG_BT_HCI_USERCHAN=y` 把 Zephyr 接到真实 HCI adapter（USB dongle），Chrome 用内置 adapter 连 `nRF5340_HR`，Patchright 截图记录心率
- 在 §6 顶部加一张 "交付件状态总表"，列所有 AC 的当前状态（PASS / SKIP / N/A / TODO）

### 2. `.github/agents/dev-workflow.agent.md` 规则更新
- **可用 Skills** 保持（ble-web-bluetooth-debugger 已列）
- **验收标准 P2** 加 AC-V1 / AC-V2
- **新增 "邀请门禁 (Invitation Gate)" 段**：明确邀请不得在以下三项全绿前发
  1. Hardware image: AC-1~AC-Q3（DK clean build + 24/24 harness）
  2. Sim image BLE run: AC-V1（bsim↔bsim HRS notify 看到）
  3. Sim image Chrome-verified: AC-V2（Chrome Web BLE 看到心率）
- **禁止事项** 加一条 "不得在邀请门禁未满足前发面试官邀请"

### 3. `verify-acceptance.sh` 扩段
仿照现有 AC-R2 `[SKIP]` 段的写法：
- AC-V1：当 `BSIM_OUT_PATH`+`BSIM_COMPONENTS_PATH` 齐备 **且** `build-bsim/.../zephyr.exe` 已存在 **且** `central_hr` 对端也已 build —— 跑 `bs_2G4_phy_v1 + zephyr.exe x2`，从 central 日志 grep `HR Measurement` / `bpm` 字样。否则 `[SKIP]`。
- AC-V2：当能探测到至少 2 个 HCI adapter（`hciconfig -a` 列出 `hci0` + `hci1`）且 `reports/ble-hr-connected.png` 存在并 mtime < 7 天 —— PASS。否则 `[SKIP]`。（把"实跑 Chrome"排除在自动脚本外，它需要人在现场）

### 4. 回归
应仍是 24/24 PASS（V1/V2 都是 SKIP），不退化。

---

## 晚间回顾

（commit 之后补）

### 完成状态
待填

### 代码/配置变更
- `REQUIREMENTS.md`：§6 顶部状态表；§6.3.2 AC-V1 + AC-V2
- `.github/agents/dev-workflow.agent.md`：P2 列表 + 邀请门禁段 + 禁止条款
- `verify-acceptance.sh`：AC-V1 / AC-V2 skip 段
- `docs/daily-logs/day-009.md`：本文件

### 明日优先事项（Day 10）
1. **拿下 AC-V1**：bsim 跑 `peripheral_hr` + `central_hr` 对（或直接拿本项目 HRS 当 peripheral、Zephyr `samples/bluetooth/central_hr` 当 central，两者都编成 bsim），用 `bs_2G4_phy_v1` 串起来，看到 notify 日志
2. （可选）为 AC-V2 先打前置：`lsusb | grep -i blu` 看看 BLE dongle 是否已在系统里认出，`hciconfig -a` 列 adapter，不行就只能等硬件准备

## Commit Message

```
feat(reqs+agent): gate interviewer invitation on sim-path BLE verification

Day 8 finished with README/CI polish and AC-R2 compile green, but the
interview deliverable still has a credibility gap: with no Nordic
hardware, clean-build + compile-only AC-R2 don't prove the BLE HRS
application actually runs. Before sending the interviewer invitation,
add two runtime-simulation acceptance criteria so the sim image is
end-to-end verified.

REQUIREMENTS.md
- §6 new top-level "交付件状态总表" (AC status snapshot)
- §6.3.2 new "硬件缺席下的运行期 BLE 验证":
  * AC-V1: bsim↔bsim HRS peripheral+central, notify log observed
  * AC-V2: native_sim + bt-hci-userchan + Chrome Web Bluetooth,
           end-to-end HR value visible in browser; Patchright skill
           drives it, screenshot stored under reports/

.github/agents/dev-workflow.agent.md
- P2 list gains AC-V1 and AC-V2
- New "Invitation Gate" section: interviewer invitation is BLOCKED
  until DK clean build + AC-V1 + AC-V2 are all green
- Forbidden-actions list: explicit "do not send invitation before the
  gate is met"

verify-acceptance.sh
- AC-V1 placeholder: runs the bsim HRS phy test when
  BSIM_OUT_PATH+BSIM_COMPONENTS_PATH are set AND build-bsim + a bsim
  `central_hr` binary are both present; otherwise [SKIP]
- AC-V2 placeholder: requires >=2 HCI adapters visible to hciconfig
  AND a recent reports/ble-hr-connected.png artifact (Chrome Web BLE
  run is out-of-band; the harness just verifies the evidence exists);
  otherwise [SKIP]

No source-code or build-config changes. Harness remains 24/24 PASS
locally (new items SKIP as expected on a machine without the sim
artifacts yet).
```
