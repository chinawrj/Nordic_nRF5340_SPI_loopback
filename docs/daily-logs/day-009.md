# Day 9 — Sim-path verification requirements + invitation gate

**Date**: 2026-04-20 (Linux Day 5)
**Iteration day**: Day 9
**Milestone**: M4 wrap-up reshaped — re-gate "interviewer invitation" on sim-path verifiability

---

## Morning plan

### Yesterday recap (Day 8)
- ✅ CI GREEN twice in a row at `f0d1a87` and `2c103bc`, workflow stable
- ✅ README now covers AC-R2 / bsim / Ubuntu 24.04 LD warning
- ✅ Local `verify-acceptance.sh` 24/24 PASS
- ⏸️ The "interviewer invitation" was proactively deferred by the user

### Today's goals (user-explicit)
User's view: for an interviewer **without hardware**, the current deliverable has weak evidence — a clean build only proves the config is correct, not that BLE HRS behaves at runtime. Before sending the invitation, close the loop once more on "sim image can run BLE". Explicit requirements:

1. **Hardware build image clean** — already held at 24/24, **regression-only**
2. **Sim image working with BLE simulation** — new AC: need bsim↔bsim running HRS peripheral ↔ HRS central
3. **Sim image verified by Chrome BLE** — new AC: need native_sim + HCI user-channel + Chrome Web Bluetooth end-to-end seeing the heart rate
4. **Bake the rule into `.github/agents/dev-workflow.agent.md`**: invitation gate
5. **Track status in `REQUIREMENTS.md`**

### Today does only structural work (actual verification deferred to Day 10+)
Implementing AC-V1 (bsim run) and AC-V2 (native_sim+Chrome) are not one-day tasks — bsim central pairing, native_sim+userchan config, USB BLE dongle testing, Patchright script polish all need work. Today's scope:

- ✅ Stand up the two new ACs + skeleton scripts (skip-when-deps-missing)
- ✅ Bake in the rule
- ✅ Status-tracking table
- ⏸️ Actual runs go to Day 10 (bsim run) and Day 11+ (Chrome BLE)

### Risks
- User picked "Both" — need two independent sim paths (bsim and native_sim), two configs, two build dirs. Don't confuse them.
- Chrome Web Bluetooth with two adapters on the same host may need BlueZ configuration via `rfkill` / `hciconfig hciN up` tweaks; cannot guarantee it works until it runs on the day
- Do not break current 24/24 because of new ACs

### Acceptance checkpoints
- [x] `REQUIREMENTS.md` §6.3 adds AC-V1 + AC-V2 + status table
- [x] `.github/agents/dev-workflow.agent.md` adds invitation gate + updates P2 list
- [x] `verify-acceptance.sh` adds AC-V1 + AC-V2 skip-unless-ready sections
- [x] Local regression still 24/24 PASS (the two new items SKIP)
- [ ] Day 9 commit + push + CI GREEN — continued after push result lands

---

## Execution log

(see commit message + subsections below)

### 1. Extend `REQUIREMENTS.md` §6
- Add §6.3.2 "Runtime BLE verification without hardware (new in Day 9+)" after the existing §6.3
- Two new ACs:
  - **AC-V1** `bsim run: HRS peripheral ↔ HRS central, notify visible` — two zephyr.exe processes connected via `bs_2G4_phy_v1` in BabbleSim, the central log shows HR notifications
  - **AC-V2** `native_sim + Chrome Web Bluetooth: end-to-end HR notify` — use `native_sim` + `CONFIG_BT_HCI_USERCHAN=y` to attach Zephyr to a real HCI adapter (USB dongle), Chrome uses the internal adapter to connect `nRF5340_HR`, Patchright screenshots the captured heart rate
- Add a "deliverable status overview" at the top of §6 listing the current status of every AC (PASS / SKIP / N/A / TODO)

### 2. `.github/agents/dev-workflow.agent.md` rule update
- **Available Skills** kept (ble-web-bluetooth-debugger already listed)
- **Acceptance P2** adds AC-V1 / AC-V2
- **New "Invitation Gate" section**: invitation must not be sent until all three are green:
  1. Hardware image: AC-1~AC-Q3 (DK clean build + 24/24 harness)
  2. Sim image BLE run: AC-V1 (bsim↔bsim HRS notify observed)
  3. Sim image Chrome-verified: AC-V2 (Chrome Web BLE sees the heart rate)
- **Forbidden actions** adds "do not send the interviewer invitation until the gate is met"

### 3. `verify-acceptance.sh` extensions
Mirroring the existing AC-R2 `[SKIP]` section:
- AC-V1: when `BSIM_OUT_PATH`+`BSIM_COMPONENTS_PATH` are set **and** `build-bsim/.../zephyr.exe` exists **and** a peer `central_hr` is built — run `bs_2G4_phy_v1 + zephyr.exe x2`, grep the central log for `HR Measurement` / `bpm`. Otherwise `[SKIP]`.
- AC-V2: when at least 2 HCI adapters are detectable (`hciconfig -a` lists `hci0` + `hci1`) and `reports/ble-hr-connected.png` exists with mtime < 7 days — PASS. Otherwise `[SKIP]`. (Keep "actually run Chrome" out of the automated script — it needs a human on site.)

### 4. Regression
Should still be 24/24 PASS (V1/V2 both SKIP), no regression.

---

## Evening review

(filled in after commit)

### Completion status
| Task | Status |
|------|--------|
| `REQUIREMENTS.md` §6.0 deliverable status overview | ✅ |
| `REQUIREMENTS.md` §6.3.2 AC-V1 + AC-V2 | ✅ |
| `.github/agents/dev-workflow.agent.md`: P2 list extension + invitation gate + forbidden clause | ✅ |
| `verify-acceptance.sh`: AC-V1 + AC-V2 skip-unless-ready sections | ✅ |
| Local regression 24/24 PASS (V1/V2 SKIP) | ✅ |
| Day 9 commit + push + CI GREEN | ✅ (`6d89e59`, run `24658965221`, 4m43s) |

Side effect: while running the harness, saw `HCI adapters seen: 2` — the user-mentioned USB BLE dongle was already recognised by the kernel; on Day 10+ when doing AC-V2, no extra driver install is needed — just `hciconfig hci1 up` and the two adapters coexist.

### Code/config changes
- `REQUIREMENTS.md`: §6 top status table; §6.3.2 AC-V1 + AC-V2
- `.github/agents/dev-workflow.agent.md`: P2 list + invitation-gate section + forbidden clause
- `verify-acceptance.sh`: AC-V1 / AC-V2 skip sections
- `docs/daily-logs/day-009.md`: this file

### Tomorrow's priorities (Day 10)
1. **Land AC-V1**: run a bsim `peripheral_hr` + `central_hr` pair (or use this project's HRS as peripheral and Zephyr `samples/bluetooth/central_hr` as central — build both as bsim), wire them through `bs_2G4_phy_v1`, and observe notify logs
2. (Optional) set up prerequisites for AC-V2: `lsusb | grep -i blu` to confirm the BLE dongle is recognised; `hciconfig -a` to list adapters; if not, wait for hardware to be ready

## Commit Message

```
feat(reqs+agent): gate interviewer invitation on sim-path BLE verification

Day 8 finished with README/CI polish and AC-R2 compile green, but the
interview deliverable still has a credibility gap: with no Nordic
hardware, clean-build + compile-only AC-R2 don't prove the BLE HRS
application actually runs. Before sending the interviewer invitation,
add two runtime-simulation acceptance criteria so the sim image is
end-to-end verified.

REQUIREMENTS.md
- §6 new top-level "deliverable status overview" (AC status snapshot)
- §6.3.2 new "runtime BLE verification without hardware":
  * AC-V1: bsim↔bsim HRS peripheral+central, notify log observed
  * AC-V2: native_sim + bt-hci-userchan + Chrome Web Bluetooth,
           end-to-end HR value visible in browser; Patchright skill
           drives it, screenshot stored under reports/

.github/agents/dev-workflow.agent.md
- P2 list gains AC-V1 and AC-V2
- New "Invitation Gate" section: interviewer invitation is BLOCKED
  until DK clean build + AC-V1 + AC-V2 are all green
- Forbidden-actions list: explicit "do not send invitation before the
  gate is met"

verify-acceptance.sh
- AC-V1 placeholder: runs the bsim HRS phy test when
  BSIM_OUT_PATH+BSIM_COMPONENTS_PATH are set AND build-bsim + a bsim
  `central_hr` binary are both present; otherwise [SKIP]
- AC-V2 placeholder: requires >=2 HCI adapters visible to hciconfig
  AND a recent reports/ble-hr-connected.png artifact (Chrome Web BLE
  run is out-of-band; the harness just verifies the evidence exists);
  otherwise [SKIP]

No source-code or build-config changes. Harness remains 24/24 PASS
locally (new items SKIP as expected on a machine without the sim
artifacts yet).
```
